[pytest]
markers =
    integration: integration tests that hit running services
testpaths = tests
python_files = test_*.py
asyncio_mode = auto

; [pytest]
; markers =
;     integration: integration tests that require docker services
; asyncio_mode = auto